workshop:
  image: node:alpine
  volumes:
    - ./:/usr/src/app
    - ./tmp/artifacts:/artifacts
  working_dir: /usr/src/app

awsdeployment:
  image: codeship/aws-deployment
  volumes:
    - ./tmp/artifacts:/artifacts
